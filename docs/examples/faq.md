# FAQ

Frequently asked questions for the example project.

## Permissions

If you encounter permission errors during installation, try:

```bash
sudo npm install -g my-project
```

Or use a Node version manager like nvm to avoid permission issues.
